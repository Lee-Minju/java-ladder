import controller.LadderController;
import domain.Ladder;
import domain.LadderDepth;
import domain.Name;
import domain.Player;
import domain.Players;
import domain.Position;
import java.util.List;
import org.junit.jupiter.api.Test;
import view.OutputView;

public class ViewTest {
/*
  @Test
  void 뷰테스트() {
    int numberOfPlayers = 4;
    Player player1 = new Player(new Name("pobi"), new Position(0));
    Player player2 = new Player(new Name("james"), new Position(1));
    Player player3 = new Player(new Name("minju"), new Position(2));
    Player player4 = new Player(new Name("jigi"), new Position(3));
    Players testPlayers = new Players(List.of(player1, player2, player3, player4));
    LadderDepth testDepth = new LadderDepth(5, numberOfPlayers);
    Ladder testLadder = new Ladder(testDepth, numberOfPlayers);

    OutputView.showUpperPhase(testPlayers, testLadder);
  }*/

}
